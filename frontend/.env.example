# Backend API base URL
VITE_API_URL=http://localhost:8000

# WebSocket URL for the live sensor feed (derived from VITE_API_URL by default — override if needed)
# VITE_WS_URL=ws://localhost:8000/ws/live
